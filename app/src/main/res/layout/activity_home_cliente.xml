<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".HomeCliente">

    <Button
        android:id="@+id/button6"
        android:layout_width="346dp"
        android:layout_height="79dp"
        android:layout_marginTop="24dp"
        android:onClick="historialPrestamos"
        android:text="Historial de préstamos"
        android:textSize="24sp"
        android:theme="@style/boton_verde_claro"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.492"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/button4" />

    <Button
        android:id="@+id/button"
        android:layout_width="346dp"
        android:layout_height="79dp"
        android:layout_marginTop="200dp"
        android:onClick="dispositivosDisponibles"
        android:text="Dispositivos disponibles"
        android:textSize="24sp"
        android:theme="@style/boton_verde_claro"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/button4"
        android:layout_width="346dp"
        android:layout_height="79dp"
        android:layout_marginTop="24dp"
        android:onClick="solicitudesPrestamos"
        android:text="Solicitudes de préstamo"
        android:textSize="24sp"
        android:theme="@style/boton_verde_claro"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/button" />

</androidx.constraintlayout.widget.ConstraintLayout>